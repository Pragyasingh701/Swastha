# Dependency directories
node_modules/
backend/node_modules/
frontend/node_modules/

# Environment files (secrets)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
backend/.env
frontend/.env
# Stray API-key files. Not used by the app (keys live in backend/.env),
# but kept ignored so a key pasted into one by hand can never be
# committed by an accidental `git add -A`.
backend/sarvam.txt
*.key.txt
uploads

# Local data & temporary fallback files
backend/data/
*.json.bak
uploads/
backend/uploads/

# Build outputs
dist/
build/
frontend/dist/

# Log files
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# OS / System files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# IDE / Editor files
.vscode/
.idea/
*.swp
*.swo
*~
*.suo
*.ntvs*
*.njsproj
*.sln

# Documentation / Temporary artifacts
docs/

# Claude Code project instructions (local-only)
CLAUDE.md
# Database backups — contain password hashes and patient health data.
# Never commit these.
backups/
